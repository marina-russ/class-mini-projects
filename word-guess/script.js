
// Array of words the user will guess
var words = [];

// init fxn called when page is loaded
function init() { };


// startGame called when the start button is clicked
function startGame() { };
// you need to render the # of blanks and timer
function startTimer() { };
function renderBlanks() { };

function checkWin() { 
//* when all blanks are !=== hidden, show a game win screen ??
};

function checkLetters(letter)

// screen to tell user the result
function winGame() { };
// when all blanks are !=== hidden, show a game win screen ??
function loseGame() { 
// if time runs out and any blanks are === hidden, show a game loss screen ??
};

// local server storage
function setWins() { };
function setLosses() { };
function getWins() { };
function getLosses() { };

// event listener to listen for keydown event
document.addEventListener("keydown", function(event) { });
//* TODO: include an eventListener to reset timer when start button is clicked

//*******************************************/




